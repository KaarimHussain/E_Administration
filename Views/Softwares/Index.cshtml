@model IEnumerable<E_Administration.Models.Software>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.SoftwareName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.SoftwareName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.SoftId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.SoftId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.SoftId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
